#ifndef COMMON_H
#define COMMON_H

// External Headers
#include <iostream>
#include <assert.h>

enum CMD {
    INVALID,
    HELP,
    GEN_RSA,
    LOAD_RSA,
    ENC_RSA,
    Q
};

using namespace std;

#endif // COMMON_H